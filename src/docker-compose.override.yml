services:
  menudb:
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=MenuSnapDb

  messagebus:
    environment:
      - RABBITMQ_DEFAULT_USER=guest
      - RABBITMQ_DEFAULT_PASS=guest

  seq:
    environment:
      - ACCEPT_EULA=Y

  minio:
    environment:
      - MINIO_ROOT_USER=minioadmin
      - MINIO_ROOT_PASSWORD=minioadmin

  api:
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=http://+:8080;http://+:8081
      - ConnectionStrings__Database=Host=menudb;Port=5432;Database=MenuSnapDb;Username=postgres;Password=postgres
      - ConnectionStrings__Redis=distributedcache:6379
      - MessageBroker__Host=amqp://messagebus:5672
      - MessageBroker__UserName=guest
      - MessageBroker__Password=guest
      - Storage__Endpoint=minio:9000
      - Storage__PublicEndpoint=localhost:9000
      - Serilog__WriteTo__1__Name=Seq
      - Serilog__WriteTo__1__Args__serverUrl=http://seq:5341
    ports:
      - "8080:8080"
      - "8081:8081"
